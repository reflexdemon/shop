loader:
    verbose: true
logging:
    level:
        org:
            springframework:
spring:
    aop:
        proxy-target-class: true
    data:
        mongodb:
#            authentication-database: reflex
            database: shop
            host: ${DB_HOST}
            username: ${DB_USER}
            password: ${DB_PASSWORD}
            port: 55787
            repositories:
                enabled: true
    jpa:
        properties:
            hibernate:
                show_sql: true
